# API 接口文档

外卖订单数据分析与可视化大屏系统 - FastAPI 接口文档。

服务默认地址：`http://127.0.0.1:8000`

Swagger 交互式文档：`http://127.0.0.1:8000/docs`

ReDoc 文档：`http://127.0.0.1:8000/redoc`

---

## 1. GET `/` - 可视化大屏

返回可视化大屏 HTML 页面。

### 请求参数

无

### 响应

- **Content-Type**: `text/html; charset=utf-8`
- **状态码**: 200

### 示例

```
GET http://127.0.0.1:8000/
```

返回完整的大屏 HTML，包含 KPI 卡片与 5 个 pyecharts 图表。

> 若大屏尚未生成（未执行 `python main.py dashboard`），返回提示页面。

---

## 2. GET `/api/health` - 健康检查

### 请求参数

无

### 响应

```json
{
  "status": "ok",
  "app_name": "外卖订单数据分析大屏",
  "version": "1.0.0"
}
```

---

## 3. GET `/api/metrics` - 全部指标

返回所有计算指标的 JSON 数据，供大屏或前端复用。

### 请求参数

无

### 响应结构

```json
{
  "overview": {
    "total_orders": 4950,
    "total_amount": 175320.50,
    "total_users": 1750,
    "total_merchants": 80,
    "total_cities": 18,
    "aov": 35.42,
    "repurchase_rate": 0.62,
    "avg_rating": 4.35,
    "avg_delivery_duration": 35.2
  },
  "peak_hours": {
    "title": "24小时订单趋势",
    "hours": ["00:00", "01:00", "...", "23:00"],
    "counts": [120, 80, "...", 200],
    "peak_hour": 12,
    "peak_count": 850
  },
  "average_order_value": {
    "total_amount": 175320.50,
    "total_orders": 4950,
    "aov": 35.42
  },
  "repurchase_rate": {
    "total_users": 1750,
    "repurchase_users": 1085,
    "repurchase_rate": 0.62
  },
  "merchant_ranking": {
    "title": "销量排行 - merchant",
    "names": ["老记餐厅", "小厨快餐", "..."],
    "order_counts": [320, 280, "..."],
    "total_amounts": [11200.0, 9800.0, "..."]
  },
  "category_ranking": {
    "title": "销量排行 - category",
    "names": ["快餐便当", "麻辣烫", "..."],
    "order_counts": [520, 480, "..."],
    "total_amounts": [18200.0, 16800.0, "..."]
  },
  "city_distribution": {
    "title": "城市订单分布",
    "cities": ["北京", "上海", "..."],
    "order_counts": [850, 820, "..."],
    "total_amounts": [30000.0, 28500.0, "..."]
  },
  "region_distribution": {
    "title": "区域订单分布",
    "regions": ["朝阳区", "浦东新区", "..."],
    "order_counts": [320, 280, "..."]
  },
  "delivery_duration_distribution": {
    "title": "配送时长分布",
    "labels": ["0-15", "15-30", "30-45", "45-60", "60-90", "90+"],
    "counts": [500, 1800, 1500, 800, 300, 50],
    "avg_duration": 35.2
  },
  "rating_distribution": {
    "title": "评分分布",
    "labels": ["1星", "2星", "3星", "4星", "5星"],
    "counts": [50, 150, 400, 1200, 3150],
    "avg_rating": 4.35
  },
  "weekday_hour_heatmap": {
    "title": "星期×小时 订单热力图",
    "weekdays": ["周一", "周二", "...", "周日"],
    "hours": ["00:00", "...", "23:00"],
    "data": [[0, 0, 15], [1, 0, 20], "..."],
    "max_value": 120
  }
}
```

### 状态码

| 状态码 | 说明                       |
|--------|----------------------------|
| 200    | 成功返回指标               |
| 404    | 清洗数据不存在，需先分析   |
| 500    | 服务器内部错误             |

---

## 4. GET `/api/orders` - 订单分页查询

分页查询订单列表，支持按城市、品类、商家过滤。

### 请求参数（Query）

| 参数       | 类型   | 必填 | 默认 | 说明              |
|------------|--------|------|------|-------------------|
| page       | int    | 否   | 1    | 页码，从 1 开始   |
| page_size  | int    | 否   | 20   | 每页条数，1-100   |
| city       | string | 否   | -    | 按城市过滤        |
| category   | string | 否   | -    | 按品类过滤        |
| merchant   | string | 否   | -    | 按商家过滤        |

### 请求示例

```
GET /api/orders?page=1&page_size=10&city=北京&category=快餐便当
```

### 响应结构

```json
{
  "total": 850,
  "page": 1,
  "page_size": 10,
  "items": [
    {
      "order_id": "TO202401010001",
      "user_id": "U10001",
      "merchant": "老记餐厅",
      "category": "快餐便当",
      "amount": 35.5,
      "order_time": "2024-06-01T12:30:00",
      "city": "北京",
      "region": "朝阳区",
      "delivery_duration": 30.0,
      "rating": 5.0
    }
  ]
}
```

### 状态码

| 状态码 | 说明                       |
|--------|----------------------------|
| 200    | 成功返回订单列表           |
| 404    | 清洗数据不存在             |
| 422    | 参数校验失败               |

---

## 5. GET `/api/cities` - 城市列表

返回所有城市及对应订单数，供前端筛选下拉使用。

### 请求参数

无

### 响应结构

```json
{
  "cities": [
    {"name": "北京", "order_count": 850},
    {"name": "上海", "order_count": 820}
  ]
}
```

---

## 6. GET `/api/categories` - 品类列表

返回所有品类及对应订单数。

### 请求参数

无

### 响应结构

```json
{
  "categories": [
    {"name": "快餐便当", "order_count": 520},
    {"name": "麻辣烫", "order_count": 480}
  ]
}
```

---

## 错误处理

所有接口统一的错误响应格式：

```json
{
  "detail": "错误描述信息",
  "error_type": "ErrorClassName"
}
```

### 常见错误

| 状态码 | error_type        | 说明                       |
|--------|-------------------|----------------------------|
| 404    | FileNotFoundError | 数据文件不存在，需先执行分析 |
| 422    | -                 | 请求参数校验失败           |
| 500    | Exception 子类    | 未处理的内部错误           |

所有错误均通过 loguru 记录到 `logs/app.log` 文件。

---

## 启动服务

```bash
# 方式一：CLI 命令
python main.py serve

# 方式二：uvicorn 直接启动
uvicorn api.main:app --host 127.0.0.1 --port 8000 --reload
```
